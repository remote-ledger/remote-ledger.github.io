{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/remote-ledger/schema/unresolved.schema.json",
  "title": "Negative results",
  "description": "Devices checked and found to have no known remote (R20, D12). Each entry is a status, not a history: the device and the date it was last checked. Lives at the repo root, not under remotes/, so remotes/**/*.json stays uniformly one-file-one-remote with no reserved names.",
  "type": "array",
  "x-order": "set",
  "items": {
    "type": "object",
    "additionalProperties": false,
    "required": ["device", "checked"],
    "properties": {
      "device": { "type": "string", "minLength": 1 },
      "checked": { "type": "string", "format": "date", "pattern": "^\\d{4}-\\d{2}-\\d{2}$" }
    }
  }
}
