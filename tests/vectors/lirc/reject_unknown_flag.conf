#
# Synthetic, written for remote-ledger's lircd port tests. Not a real remote.
#
# lircd rejects this file. Flags are matched exactly (ignoring case).
#
begin remote
  name  typo
  bits            8
  flags SPACE_ENC|CONST_LENGHT
  one           500  1500
  zero          500   500
  ptrail        500
  gap           40000
      begin codes
          KEY_A                    0x0A
      end codes
end remote
